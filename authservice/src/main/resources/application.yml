server:
  port: 2030
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:2016/eureka

  instance:
    hostname: localhost
spring:
  application:
    name: auth-service

